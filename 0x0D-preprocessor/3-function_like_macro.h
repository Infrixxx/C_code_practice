#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

#define ABS(X) ((X) < 0 ? -X:X)   /*if x is negative we get positive, if positive, we get positive*/

#endif /*end if for Function like macro def*/
