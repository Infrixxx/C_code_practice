#include <stdio.h>

/**
 * main - prints a string literal using printf
 * Return : 0 upon success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
